export type Post = {
	title: string
	slug: string
	description: string
	date: string
	tags: string[]
	draft: boolean
	image: string
}

export type ExampleImage = {
	fileName: string,
	title: string,
}
